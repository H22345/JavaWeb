# JavaSE

### 1.API应用程序编辑接口

​		简单理解，就是别人已经写好的东西，我们不需要自己编写，直接使用即可，在Java中指的就是JDK中提供的各种功能的Java类（例如Scanner,Random），这些类将底层的实现封装了起来，我们不需要关心这些类是如何实现的，只需要学习这些类如何使用即可，将来还会学习其他知识，还会有很多的API

***

### 2.API

#### (1)常用API

​		1.System: System也是一个工具类，提供了一些与系统相关的方法

```java
public static void exit(int status)  //status指状态码，0表示当前虚拟机是正常停止，System.exit(0);非0则表示当前虚拟机异常停止
public static long currentTimeMillis()  //返回当前系统的时间毫秒值形式
public static void arraycopy(数据源数组,起始索引,目的地数组,起始索引,拷贝个数)  // 数组拷贝
```

​		2.Runtime: 表示当前虚拟机的运行环境，该类中的方法不是静态的，因此需要先获取其对象，但因为其构造方法已经私有化了，因此不能直接new创建对象，需要用getRuntime()的方法获取：

```java
Runtime r1 = Runtime.getRuntime();
```

这样设计的原因：在一个电脑中仅有一个运行环境，创建多个对象没有什么实际意义

```java
public static Runtime getRuntime()  //当前系统的运行环境对象
public void exit(int status)  //停止虚拟机，System.exit()在底层实际上调用的就是Runtime里的exit()方法
public int availableProcessors()  //获取CPU的线程数
public long maxMemory()  //JVM能从系统中获取总内存大小(单位byte)
public long totalMemory()  //JVM已经从系统中获取总内存大小(单位byte)
public long freeMemory()  //JVM剩余内存大小(单位byte)
public Process exec(String command)  //运行cmd命令
/*cmd操作：
shutdown:关机(需要加上参数才能运行)
-s:默认在1分钟后关机
-s -t:指定关机时间，单位为秒
-a:取消关机操作
-r:关机并重启
*/
```

​		3.Object: 是Java中的顶级父类。所有的类都直接或间接地继承于Object类。Object类中的方法可以被所有子类访问，所以我们要学习Object类和其中的方法。因为不存在所有类的共性，因此在Object类当中没有成员变量，因此没有带参的构造方法，只有一个空参构造：

```java
public Object()  //空参构造
```

```java
public String toString()  //返回对象的字符串表示形式，包名+类名+@+地址值(经过一系列复杂运算变成16进制)，因此想要获取类中的成员属性的话可以注解@Override并重写toString()方法，或者使用PDG插件
public boolean equals(Object obj)  //比较两个对象是否相等，默认比较的是地址值(没什么意义)；如果想要比较对象属性，可以重写该方法，用Alt+Insert快捷键->equals and hashCode()->Next……->删除暂时无用的hashCode()即可
    /*补充：
    当使用equals方法比较String类型和StringBuilder类型时，一定返回false,但原理不同。s.equals(sb)调用的是String当中的，被重写过的方法：先比较；两者是否为同一对象，不是再判断传递的参数是不是字符串，是则比较内部属性是否相同，不是则直接返回false，因为StringBuilder不是字符串，因此返回false；而sb.equals(s)调用的是StringBuilder继承的AbstractStringBuilder继承的Object顶级父类中的初始equals方法，未被重写，因此只比较地址值，所有结果返回false
    */
protected Object clone()  //对象克隆，也叫对象拷贝，对象复制。因为clone方法是被protected修饰的，即只能被本包中的类和其他包中的子类调用，并且Object类是定义在Java.lang包下的，而我们又不能将代码写在lang包下，因此使用clone方法必须在对象所在的类中重写，直接return super.clone()即可，也就是调用父类的clone方法；且需要在该Javabean类中实现一个Cloneable标记性接口，表示当前类的对象可被克隆；因为clone方法在源码当中返回的是Object对象，所以在测试类中进行克隆操作时需要用到一个强转操作(tip:在克隆时不需要再手动创建对象，clone方法在底层会帮我们创建一个对象并把原对象的数据拷贝过来)
    /*补充：
    克隆分为浅克隆和深克隆两种。共同点是都会先创建一个新对象并将基本数据类型拷贝过来；区别是浅克隆会将引用数据类型的地址值拷贝，而深克隆是再创建一个引用数据类型对象，将原引用数据类型的具体数据拷贝到新引用数据类型对象，而克隆出来的对象则会使用新的引用数据类型对象的地址值。(tip:字符串其实也是引用数据类型，但它有一个特点，只要不是手动new创建的，都会在串池中进行管理并会复用，也就是说即便是深拷贝，还是会复用String数据类型的地址值)而Object当中的克隆方法属于浅克隆，想要深克隆，需要手动在重写方法中进行new创建一个新的引用数据类型，利用方法(如arraycopy()等)或者对象名.fori进行引用数据类型的复制，再在方法内部调用一次Object中的克隆方法(即"引用数据类型 对象名 = (引用数据类型) super.clone();")，注意这里需要一次强转；最后返回该对象即可
    事实上，深克隆可以运用到第三方工具，无需手写，只需要将对应工具代码的jar包导入到项目当中(一般导入到lib中)就可供使用了。使用方法(假设需要克隆的对象为u1，所属的类为User)：
    Gson gson = new Gson();  //先创建第三方工具的对象
    String s = gson.toJson(u1);  //将对象变为一个字符串
    User user = gson.fromJson(s,User.class);  //将字符串变回对象即可完成克隆
    主要原理是将对象转换为JSON字符串，然后再从JSON字符串重建了新的对象
    */
```

​		4.Objects: 是一个工具类，提供了一些方法去完成一些功能

```java
public static boolean equals(Object a,Object b)  //先做非空判断，比较两个对象。以Student类为例，因为是静态方法，所以可以直接通过类名进行调用：Objects.equals(s1,s2)，方法底层会先判断s1是否为null，如果为null，直接返回false，如果s1不为null，则利用s1再次调用equals方法，此时s1是Student类型，所以最终还是会调用Student中的equals方法，而如果没有重写的话，则比较地址值，如果重写了，就比较属性值
public static boolean isNull(Object obj)  //判断对象是否为null，为null返回true，反之
public static boolean nonNull(Object obj)  //判断对象是否为null，结果与isNull方法相反
```

​		5.BigInteger: 在Java中，整数有四种类型(在底层占用字节个数)：byte(1个字节) ,short(2个字节) ,int(4个字节) ,long(8个字节)，以最占用字节个数最多的long类型为例，用二进制表示可存储8*8=64位数字，即最大为01111111  11111111  11111111  11111111  11111111  11111111  11111111  11111111，转换成十进制就是9223372036854775807，共19位数字，当需要表示的整数比这还大的话，就要使用到BigInteger。虽然BigInteger有上限，但其上限非常大，可以视为无限

```java
public BigInteger(int num,Random rnd)  //获取随机大整数，范围：[0~2的num次方-1]
public BigInteger(String val)  //获取指定的大整数-常用-超出long
public BigInteger(String val,int radix)  //获取指定进制的大整数，例如sout(BigInteger(100,2));则表示打印二进制的100所代表的整数，也就是4；而当打印BigInteger(102,2)时则会报错。因此：字符串中的数字必须是整数，字符串中的数字必须要跟进制吻合

public static BigInteger valueOf(long val)  //静态方法获取BigInteger的对象，内部有优化，会提前把-16~16先创建BigInteger的对象，如果多次获取是不会重新创建的。这个方法的不同之处在于能表示的范围比较小，只能在long的取值范围之内-常用-没超出long
add(BigInteger bi)  //加
subtract(BigInteger bi)  //减
multiply(BigInteger bi)  //乘
divide(BigInteger bi)  //除(仅获取商)
BigInteger[] divideAndRemainder(BigInteger bi)  //除(获取商和余数，返回值是一个BigInteger数组，0索引是商，1索引是余数)
equals(Object x)  //比较，在BigInteger类中已重写方法，比较内部属性值
max/min(BigInteger bi)  //比较大小并返回对应值所存储于的对象的地址值，不会创建新的对象
```

​		需要注意的是，对象一旦创建，里面的数据不能发生改变，而且BigInteger对象之间不能进行简单的四则运算，而是要通过方法调用进行计算，例如BigInteger result = bd1.add(bd2);其中的add就是BigInteger提供的用于相加的方法。只要进行计算都会产生一个新的BigInteger对象，哪怕计算的结果在-16~16之间也不会影响其创建一个新的对象来存储这个数字

​		6.BigDecimal: 作用是表示较大的小数并解决小数运算精度失真问题，因为在小数计算的底层二进制源码当中，将整数部分和小数部分进行了分隔，整数部分是用底数乘基数的全次幂再相加，例如01000101是0×2^7 + 1×2^6 + 0×2^5 + 0×2^4 + 0×2^3 + 1×2^2 + 0×2^1 + 1×2^0 = 0 + 64 + 0 + 0 + 0 + 4 + 0 + 1 = 69，而小数部分也是用底数乘基数的全次幂，计算出每一位的十进制再相加，例如111是1×2^(-1) + 1×2^(-2) + 1×2^(-3) = 0.5 + 0.25 + 0.125 = 0.875，但部分小数的小数部分转换成二进制的位数会很长，可能高达几十位，而float和double类型在底层中所占的字节数是有限的，float占用字节数为4，总bit位数为32，小数部分bit位数为23；double占用字节数为8，总bit位数为64，小数部分bit位数为52。超出部分只能舍弃了，例如0.226的小数部分的二进制有55位，因此结果会不精确。

```java
public BigDecimal divide(BigDecimal val,精确几位,舍入模式)  //除法
/*
以下枚举常量代表的舍入模式单独归于RoundingMode
UP:0，远离零方向舍入的舍入模式
DOWN:1，向零方向舍入的舍入模式
CEILING:2，向正无穷方向舍入的舍入模式
FLOOR:3，向负无穷方向舍入的舍入模式
HALF_UP:4，四舍五入(5.5->6)
HALF_DOWN:5，五舍六入(5.5->5)
*/
```

拓展: BigInteger和BigDecimal的底层存储方式不同，前者是每32位分段存储到数组当中的，例如[1,-2147483648,0]，0索引表示正负，后面的索引则是将数字转化成二进制形式后，再按照32位二进制位数分成若干段，每段二进制位数再转化成十进制存储的；而后者则是依次获取new创建的字符串中的每个字符，并对应ACSII码表中对应的值存储到一个byte类型的数组当中，例如"0.226"->[48,46,50,50,54]，如果有负号，也会把负号对应的ASCII码表值存储过来，虽然都是数组，但含义完全不同。

​		<u>**7.正则表达式:**</u> 
​		<u>Pattern</u>: 一个在java.util.regex.Pattern包下的类，其对象即正则表达式，可以校验字符串是否满足一定的<u>规则</u>，并用来校验数据格式的合法性。其本质是将各种各样的规则拼接成一个整体，即正则表达式。在实际开发中很少自己写正则表达式，因此对于正则表达式只需要理解以下部分即可：
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2025-12-31 015421.png)

​		转义字符\：改变其后面那个字符原本的含义，在正则表达式中，两个\表示一个\，因为第一个\会把第二个\变为一个普通的\，这是必须要写的，因为只有一个\时会将后一个。转义字符作用：

```java
System.out.println("\"");  //能打印出单独一个"符号
System.out.println("C:\\Users\\……");  //路径中通常只有一个\，而这个\如果写在字符串当中很可能会改变其后面那个路径的首字母的含义，因此该\前需要再加一个\用于将该\改变成普普通通的\
```

​		<u>Matcher</u>: 文本匹配器，在java.util.regex.Matcher包下，作用是按照正则表达式的规则去读取字符串，而且是从头开始读取，在大串中去找符合匹配规则的子串。

​		8.Date时间类: 

```java
public class Date(){  //Java底层同义源码
    private long time;  //当前时间的毫秒值
    public Date(){  //空参构造方法
        this.time = System.currentTimeMillis();
    }
    public Date(long 给成员变量time){  //带参构造方法，可以传递一个time再赋值
        this.time = time;
    }
    setTime()/getTime()
}

//创建对象表示一个指定的时间
Date d = new Date(0L);  //因为此处应填入一个long类型数据，因此最好在后面加上一个L
```

#### (2)常用操作

​		1.断点调试: 即debug模式，作用是查看程序是如何一步步运行的。先打一个断电，再右键debug，然后step into来进行下一行即可展示运行每一行时的底层逻辑。
​		2.源码查询: 使用快捷键Ctrl+B跟进可查询源码，研究代码底层逻辑

***

### 3.导包

  某些Java类在设计好的包中，我们需要导用，例如Random方法在java.util包中，所以如果我们需要使用包中的Random方法的话则在测试类的main外使用import java.util.Random指令进行导包

***

### 4.Java.lang

​		这是Java的核心包，其中有很多常用方法，例如String是Java定义好的一个类，在Java.lang包中，所以使用的时候不需要导包

***

### 5.String字符串

​		Java中的所有字符串文字都被视为String类的对象，字符串不可变，它们的值在创建后不能被更改，在开发中比较常用的创建字符串的方法有：
　　　　String s = “XXX”;//常规创建实例对象
　　　　……		
　　注意：当使用双引号直接赋值时，系统会检查该字符串在串池中是否存在。不存在时创建新的，存在时则复用(利用了字符串不可更改的性质，节约了内存空间)。
　　　　char[] chs ={‘a’,’b’,’c’};//静态赋值
　　　　String s = new String(chs);//此时会依照数组索引顺序进行拼接形成字符串，且因为是利用new关键字创建的字符串，所以会在堆内存开辟一个新的空间进行字符串的存储，即使创建另外一个字符串来存储相同的字符数组也不会复用空间。
　　　　或
　　　　

```java
Scanner sc = new Scanner(System.in);
char[] chs = new char[5];
for(int i=0;i<chs.length;i++){
	chs [i]=sc.next().charAt(0);//循环遍历数组，动态赋值
}
```

　　        ……
　　　　或
　　　　

```java
byte[] bytes = new byte[5];
bytes.fori{
	bytes.[i]=sc.nextInt();
}
String s = new String(bytes);//创建实例对象
```

String Table(串池-字符串常量池)
		直接赋值的字符串存储在串池，在JDK7之前，串池归属方法区存储；而new创建的字符串则存储在堆内存。而从JDK7版本开始，串池从方法区挪到了堆内存。但无论在哪，其运行机制不变。注意：字符串常量池的本质是记录字符串常量引用的哈希表，而非独立的空间。

String字符串比较

  

```java
String s1 =”abc”;
String s2 =”abc”;
System.out.println(s1==s2);//true

String s1 = new String(“abc”);
String s2 =”abc”;
System.out.println(s1==s2);//false
```

  ==号比的到底是什么？当比较的是基本数据类型时，会比较具体的值是否相等；当比较引用数据类型时，会比较数据的地址值是否相等。
		具体执行逻辑：
　　(1)String s1 = new String(“abc”);
　　　new在执行时会先检查String Table中有没有”abc”，若没有则在堆中创建一个”abc”常量对象，将其引用存入String Table，然后，new操作会再创建一个新的”abc”对象(与String Table指向的常亮对象是两个独立实例，地址不同)，最终s1指向的是new创建的新对象(堆中独立实例)。
　　(2)String s2 =”abc”;
　　 执行时只检查String Table，发现已有”abc”的引用，直接返回该引用(注意：不是返回串池中记录的字符串，而是串池指向的在堆空间里的字符串常量”原件”，该”原件”是不同于new创建的新对象的，不过它们都在堆空间中)因此s2指向的是String Table中存储的常亮对象(并非s1指向的new创建的对象)。
　　简单来说，new创建的s1是记录堆里面的地址值(指向堆中的对象)，直接赋值的s2是记录串池中的地址值(指向堆中的字符串常量)。
　　
　　字符串比较的方法
·boolean equals方法(要比较的字符串)——完全一样结果才是true，否则为false
·boolean equalsIgnoreCase(要比较的字符串)——忽略大小写的比较
　　这两种字符串的比较方法返回的都是boolean数据类型，可以定义一个boolean类型变量存储结果：
·boolean flag1 = s1.equals(s2);
·boolean flag2 = s1.equalsIgnoreCase(s2);

​		String字符串输入

　　

```java
//输入字符串
Scanner sc = new Scanner(System.in);
String str = sc.next();
//进行遍历
str.length().fori{			//利用了length()方法获取字符串长度-int
char c = str.charAt(i); 	//利用了charAt(int i)方法获取字符索引
sout(c);
}
```


  	注意：上面的charAt(int i)方法的返回值是char类型，读取的是i索引对应的字符并直接返回，所以可以将返回值直接代入char类型的变量

String字符串截取

·String substring(int beginIndex,int endIndex)截取
  注意点：<u>包头不包尾，包左不包右</u>
          只有返回值才是截取的小串
·String substring(int beginIndex)截取到末尾

String字符串替换
·String replace(旧值,新值)替换
  注意点：只有返回值才是替换后的结果

StringBuilder概述(对象)
		StringBuilder可以看成是一个容器，一般用于字符串的拼接和字符串的反转，创建之后里面的内容是可变的，作用是提高字符串的操作效率，因为普通的字符串拼接是需要在每次拼接的过程中重新创建的，例如：

```java
String s1=”aaa”;
String s2=”bbb”;
String s3=”ccc”;
String s4=”ddd”;
String s=s1+s2+s3;
```

​		在运行该程序时，会先创建一个字符串为aaabbb，再创建一个字符串aaabbbccc，最后才会形成aaabbbcccddd，期间会创建多个字符串，当字符串数量太多时会极大地影响编译器运行效率
  相关方法：
·public StringBuilder append(任意类型)——添加数据，并返回对象本身
·public StringBuilder reverse()——反转容器中的内容
·public int length()——返回长度(字符出现的个数)
·public String toString()——通过toString()就可以实现把StringBuilder转换为String

​		在运行该程序时，会先创建一个字符串为aaabbb，再创建一个字符串aaabbbccc，最后才会形成aaabbbcccddd，期间会创建多个字符串，当字符串数量太多时会极大地影响编译器运行效率
  相关方法：
·public StringBuilder append(任意类型)——添加数据，并返回对象本身
·public StringBuilder reverse()——反转容器中的内容
·public int length()——返回长度(字符出现的个数)
·public String toString()——通过toString()就可以实现把StringBuilder转换为String

StringJoiner概述(可以高效、方便的拼接字符串)
  和StringBuilder一样，也可以看成是一个容器，创建之后里面的内容是可变的，作用是提高字符串的操作效率，而且代码编写特别简洁，但是目前市场上很少有人用，因为是JDK8才出现的
·public StringJoiner(间隔符号)
·public StringJoiner(间隔符号,开始符号,结束符号)
例如:

```java
StringJoiner sj=new StringJoiner(“---”);//1---2---
sj.add(“a”).add(“b”).add(“c”);//StringJoiner的拼接是add(任意类型)方法，区别于StringBuilder的
							  //append()方法
System.out.println(sj);
```

  此时终端会显示a---b---c；

字符串原理
		字符串存储的内存原理：直接赋值会复用字符串常量池中的，new出来的不会复用，而是开辟一个新的空间
  ==号比较的到底是什么：基本数据类型比较数据值，引用数据类型比较地址值
		字符串拼接的底层原理：字符串拼接时用+和设置变量会在底层创建很多对象，哪怕是用StringBuilder方法，当处理多行也会需要进行多次预估字符数来创建数组存储，这样太浪费时间和性能了，因此尽量用StringBuilder或StringJoiner
		StringBuilder提高效率原理图：StringBuilder源码分析:
·默认创建一个长度为16的字节数组
·添加的内容长度小于16——直接存
·添加的内容长度大于16会扩容(到34字节，也就是16*2+2字节)
·如果扩容后还不够，以实际为准
(注意:查询StringBuilder类型的容量用capacity()方法(返回值类型int)，查询其长度则用length()方法(返回值类型同样为int)，容量<=长度)
		StringBuilder与串池的联系:StringBuilder始终在堆中独立存在，仅toString()+intern()能关联串池
		补充:字符串底层优化原理——字符串直接相加(过程无变量时)会触发字符串底层优化原理，将拼接后的结果直接存储到串池当中

String字符串方法补充:

```java
String str1 = new String;
String str2 = new String;
boolean flag1 = str1.startsWith(str2);  //判断str1字符串是不是以str2字符串开头的，返回值是boolean类型
boolean flag2 = str1.matches(String数据类型的regex);  //判断str1字符串是否满足正则表达式，返回值是boolean类型。或者先创建一个字符串数据类型regex用来存储正则表达式，然后在matches方法的实参中填入regex也可以
```



***

### 6.static静态

(1)驼峰命名法
(2)私有化构造方法
(3)定义工具类方法时需static修饰，可全局调用方法
(4)测试类调用时采用”返回值类型 变量名=工具类名.方法名(实参)”的形式调用，无返回值则无需定义变量接收
(5)static和final组合标记全局不可变常量

***

### 7.继承(权限等级[宽]public>protected>default>private[窄、严格])

  Java只支持单继承，不支持多继承，但支持多层继承
  每一个类都直接或者间接继承于Object：当无”extends”继承关键字时，JVM虚拟机会默认该类继承Object，否则间接继承
  在设计子类父类时，注意核心：
(1)共性内容抽取到父类，独特内容保留在子类
(2)子类是父类中的一种
(3)设计时从下往上推，写代码时从上往下写
(4)父类方法的权限修饰符应该是public，[多态+通用复用]，否则子类无法访问
(5)父类属性的权限修饰符则应该是private封装属性，依靠getter/setter对外交互(仅常量可public)
  难点：子类到底能继承父类中的哪些内容？
<u>·构造方法：非私有(不能继承)   private(不能继承)</u>
　　因为父类中的构造方法是需要用到类名的，例如”public Fu(){}”空参构造，子类复用时则会使构造方法中的Fu与子类类名Zi冲突，因此无论如何父类的构造方法都不能被子类继承，因此子类需自定义构造方法，或则JVM虚拟机调用父类的构造方法(默认空参构造，可通过super()显示调用)，但这不是”继承”，而是构造器的链式调用规则，Java从语法层面就规定所有构造方法都不能被继承。**<u>之所以是默认空参构造，是因为顶级父类Object中只有无参构造方法</u>**
<u>·成员变量：非私有(能继承)     private(能继承)</u>
  需要注意的是，父类中private修饰的成员变量在子类中是不能直接调用的，因为子类创建在堆中的对象的存储父类成员变量那部分是通过private修饰符限制访问的，所以不能对其直接操作，需通过父类的get/set方法间接操作
<u>·成员方法：非私有(能继承)     private(不能继承)(不严谨)</u>
  事实上，父类的成员方法无论是用什么修饰都能被继承，只不过对于父类中private修饰的成员方法，子类无法访问、无法重写，也无法直接调用(<u>“无法重写”和”无法直接调用”的本质是”无法访问”的具体表现</u>)，因此同样也无法添加到虚方法表中。对于子类调用父类方法，最高父类(非Object)会将非private修饰、非static静态修饰、非final关键字修饰的虚方法进行抽取并放入虚方法表当中，传递给其直接子类，而第二层父类则会在其父类的虚方法表的基础上再添加自己类中的虚方法，再传递给其子类，依次传递(准确描述、补充:<u>Object类是所有类的根父类，也会参与虚方法表的构建；且”虚方法”本身就是指可重写的实例方法，父类的这些方法会被纳入虚方法表，子类会在父类虚方法表基础上叠加自身的可重写方法，而非”最高父类(非Object)”单独处理</u>)所以子类在调用父类方法时会直接从虚方法中调用，需要注意的是：只有父类中的虚方法才能被子类继承

tip：执行程序时方法区中加载字节码文件，当创建子类对象时，类的加载顺序是：先父类，后子类，且Object是所有类的根父类，会最先被加载；在用new关键字创建对象时，如果对象所属的类属于子类，则会将堆中创建的空间一分为二(准确描述:<u>实际上子类对象的堆内存是**一个连续的整体**，并非物理上”切割”，只是逻辑上包含父类的成员变量区域和子类自身的成员变量区域)，一部分用于存储父类的成员变量，一部分用于存储子类的成员变量</u>，随后对这些成员变量进行默认初始化值的操作。在后续对该对象成员变量进行操作时，会先对存储子类成员变量的那部分空间进行查找，没找到才会再对存储父类成员变量的那部分空间进行查找(准确描述:成员变量查找时，遵循”先子类，后父类”的顺序，是JVM的字段解析规则，和内存物理分区无关)

总结：继承的本质是”子类对象包含父类的成员”，哪怕是private变量，也会作为子类对象的一部分存在于堆内存中，private仅限制直接访问权限，不影响”继承”这个事实

***

### 8.多态

  多态，即同种类型的对象表现出的不同形态：
(1)父类引用指向父类对象；
(2)子类引用指向子类对象；
(3)父类引用指向子类对象。*
  多态必须前提：需要有继承/实现(与接口有关)关系，多态要求子类重写父类方法时，方法权限不能比父类更严格。所以若父类使用protected/default时，子类重写只能用同等级或更宽权限，限制灵活性；用public，子类可直接继承/重写，无权限束缚，多态调用(父类引用指向子类对象)能正常生效
  多态的使用场景十分广泛，作为面向对象编程的三大特点(封装、继承和多态)之一，例如需要创建一个多个子类共同使用的方法，可以使用父类引用指向子类对象创建对象，而方法的形参则直接选择父类即可，并且在该方法中需要调用父类方法时还可以直接采用”形参名.方法名()”，只要各个子类对于父类中该方法进行了重写，那么就能够执行对应的子类方法，例如

```java
public void register(Person p){
    p.show();
}
```

  Person是父类，可以有Teacher、Student、Administrator等子类，只需在创建对象时传递对象给父类类型即可：
Person tea=new Teacher();
Person stu=new Student();
Person adm=new Administrator();
  在以上三个语句中，左侧是父类Person类型的引用变量，右侧是子类的实例对象，是父类引用指向子类对象的典型写法
  多态的优势：
·在多态形式下，右边对象可以实现解耦合，便于扩展和维护
<u>·定义方法的时候，使用父类类型作为参数，可以接受所有子类对象</u>，体现多态的扩展性与便利
  多态的弊端：
·无法直接访问子类特有成员，子类独有功能需要强制转型才能使用，增加代码操作成本，且该转型存在安全风险，需额外用instanceof判断，冗余代码且易遗漏，并会使性能略有损耗
(tip:instanceof使用方法：boolean flag=变量名(即对象) instanceof 类名，用于判断变量名存储的对象是否为对应的类，是则返回true，而在JDK 14后添加了一个新特性，例如:

```java
if(a instanceof Dog d){
　　d.lookHome();
}
```

  意为判断a对象是否为Dog类型，是则强转为Dog类型，转换之后变量名为d，并执行Dog类特有方法lookHome()，不是则直接返回false)
  多态调用成员的特点：
<u>·变量调用：编译看左边，运行也看左边；</u>
<u>·方法调用：编译看左边，运行看右边</u>
  变量调用：例如对于父类引用指向子类对象，对该对象编写”变量名.属性名”的代码时，编译方面：会看左边的父类中有没有这个变量，有则编译成功，无则编译失败；运行方面：实际获取的是左边父类中成员变量的值。
  方法调用：例如对于父类引用指向子类对象，对该对象编写”变量名.方法名()”的代码时，编译方面：会看左边的父类中有没有这个方法，有则编译成功，无则编译失败；运行方面，实际运行的是子类中的方法(若重写过则调用重写后的方法，否则调用继承下来的父类的方法)
　　理解方式：在子类的对象中，会把父类的成员变量也继承下来，因此对象中实则有父类和子类两个name，用父类类型调用时，自然调用对象中的父类name；而对于方法而言，子类的重写是会把子类中vtable的相应方法覆盖的，又因为new创建的对象是子类的，所以会调用子类(重写后)的方法
　　tip：在加载字节码文件时，优先加载父类再加载子类(包括Object)，会加载类的成员变量和成员方法以及vtable

  意为判断a对象是否为Dog类型，是则强转为Dog类型，转换之后变量名为d，并执行Dog类特有方法lookHome()，不是则直接返回false)
  多态调用成员的特点：
<u>·变量调用：编译看左边，运行也看左边；</u>
<u>·方法调用：编译看左边，运行看右边</u>
  变量调用：例如对于父类引用指向子类对象，对该对象编写”变量名.属性名”的代码时，编译方面：会看左边的父类中有没有这个变量，有则编译成功，无则编译失败；运行方面：实际获取的是左边父类中成员变量的值。
  方法调用：例如对于父类引用指向子类对象，对该对象编写”变量名.方法名()”的代码时，编译方面：会看左边的父类中有没有这个方法，有则编译成功，无则编译失败；运行方面，实际运行的是子类中的方法(若重写过则调用重写后的方法，否则调用继承下来的父类的方法)
　　理解方式：在子类的对象中，会把父类的成员变量也继承下来，因此对象中实则有父类和子类两个name，用父类类型调用时，自然调用对象中的父类name；而对于方法而言，子类的重写是会把子类中vtable的相应方法覆盖的，又因为new创建的对象是子类的，所以会调用子类(重写后)的方法
　　tip：在加载字节码文件时，优先加载父类再加载子类(包括Object)，会加载类的成员变量和成员方法以及vtable

***

### 9.包

  包就是文件夹。用来管理各种不同功能的Java类，方便后期代码维护
·包名的规则：公司域名反写+包的作用，需要全部英文小写，见名知意
·全类名=包名+类名，例如:<u>com.itheima.domain</u> . <u>Student</u>
　　												  (包名)			 	  (类名)
　　在使用其他类时，需要使用全类名，例如:

```java
public class Test{
　　pubilc static void main(String[] args){
　　	com.itheima.domain.Student stu=new com.itheima.domain.Student();
　　}
}
```

　　这样过于麻烦，因此可以利用”import 包名.类名;”进行导包，而当使用同一个包中的类时，不需要导包；使用java.lang包中的类时，不需要导包；其他情况都需要导包，<u>如果同时使用两个包中的同类名，必须需要用全类名</u>

　　这样过于麻烦，因此可以利用”import 包名.类名;”进行导包，而当使用同一个包中的类时，不需要导包；使用java.lang包中的类时，不需要导包；其他情况都需要导包，如果同时使用两个包中的同类名，必须需要用全类名

16.final关键字
·修饰方法时，表明该方法是最终方法，不能被重写
·修饰类时，表明该类是最终类，不能被继承
·修饰变量时，叫做常量，只能被赋值一次，不能被修改
(tip:实际开发中，常量一般作为系统的配置信息，方便维护，提高可读性，常量的命名如果是单个单词：全部大写；多个单词：全部大写，单词之间用下划线隔开。这也是为什么之前的变量名方法名都不建议用下划线，因为这是专门为常量准备的。细节：<u>final修饰的变量是基本类型：那么变量存储的数据值不能发生改变；final修饰的变量是引用类型：那么变量存储的地址值不能发生改变，但在对象内部存储的属性值可以发生改变</u>)
  当需要定义一个不可改变的规则时，则使用final修饰，例如Object类中就有方法是这样设计的

***

### 10.权限修饰符

<img src="C:\Users\22345\Pictures\Screenshots\屏幕截图 2025-12-14 163039.png" style="zoom: 50%;" />

·private:只能自己用
·默认:只能本包中使用，其他包哪怕导包了也不能使用
·protected:受保护的，其他包中的子类也能用
·public:公共的，所有地方都能用
  实际开发中，一般只用private和public
·成员变量私有
·方法公开
  特例:如果方法中的代码是抽取其他方法中共性代码，这个方法一般也私有

***

### 11.代码块

·局部代码块:方法内部的大括号，用于提前结束变量的生命周期(已淘汰)
·构造代码块:一般写在成员位置的代码块，作用是把多个构造方法中重复的代码抽取出来；执行时机：创建本类对象时，构造代码块优先于构造方法执行，且每次创建对象的时候都会执行一次(不够灵活)



·静态代码块:(需要重点掌握)
  格式:static{}
  特点:需要通过static关键字修饰，随着类的加载而加载，并且会自动触发、只执行一次
  使用场景:在类加载的时候，做一些数据初始化的时候使用

***

### 12.抽象类(abstract class)

·抽象方法
　　将共性的行为(方法)抽取到父类之后，由于每一个子类执行的内容是不一样的，所以，在父类中不能确定具体的方法体，该方法就可以定义为抽象方法
　　抽象方法的格式有固定要求，核心是只有方法签名，没有方法体，具体语法结构如下:
　　<u>[访问修饰符] abstract 返回值类型 方法名(参数列表);</u>
　　需要注意的是，不能和static/final/private同时使用(这些关键字会和抽象方法”必须被子类重写”的特性冲突)，且方法末尾直接用分号结尾，没有{}包裹的方法体
·抽象类:如果一个类中存在抽象方法，那么该类就必须声明为抽象类
·抽象类的定义格式
　　public abstract class 类名{}//
·抽象类和抽象方法的注意事项:
(1)抽象类不能实例化，即不能创建对象，但是**可以在抽象类的静态方法里创建自己的子类对象**，并**利用方法进行实例化后返回**，“生产”出一个能直接用的子类对象
(2)抽象类中不一定有抽象方法，有抽象方法的类一定是抽象类
(3)可以有构造方法 
(4)抽象类的子类
　　要么重写抽象类中所有抽象方法
　　要么是抽象类

---

### 13.接口(interface)(通过implements关键字实现)

#### 		一、接口概述

​		<u>在Java的类型层级中，接口和类是同层级的核心类型，二者都属于独立的引用类型，地位并列</u>，但本质和用途截然不同，接口的核心作用包含两点:
·约定行为标准
　　接口只定义”要做什么”，不规定”怎么做”，让不同的类可以按照统一标准实现功能，比如Flyable接口约定fly()方法，小鸟、飞机、无人机都能各自实现
·降低代码耦合
　　依赖接口编程而非具体类，能降低模块间的耦合度，比如调用方只需要调用Flyable中的fly()方法，不用关心是哪个类在执行，便于后续替换实现类
·补充单继承局限
　　像Java这种只支持单继承的语言，，一个类可以实现多个接口，从而拥有多种行为能力(比如一个类同时实现Flyable和Swimmable)，突破了单继承的限制；但如果实现了多个接口，而<u>多个接口中存在相同名字的默认方法，则实现类必须对该方法进行重写</u>，否则无法确定调用的是哪个接口的默认方法

#### 		二、接口中成员的特点

·成员变量: 
　　只能是常量
　　默认修饰符:public static final
·构造方法:
　　没有
·成员方法:
　　只能是抽象方法(JDK 7及以前)
　　抽象方法默认的修饰符:public abstract，可以只写”返回值类型 方法名(形参)”
　　JDK 8/9以后: 
　　
　　(1)新增了”<u>接口中可以定义默认方法</u>”的特性，是为了在接口升级(需要添加新的方法)时，为了兼容性而使用的，因为如果使用该接口的类如果不重写新添加的方法则会报错，而如果在接口中写有方法体的方法则实现接口的类不会报错，当需要具体调用方法时再重写即可
　　接口中默认方法的定义格式:

```java
public default 返回值类型 方法名(参数列表){}
```

(tip: 默认方法不是抽象方法，所以不强制被重写。但是如果被重写，重写的时候去掉default关键字(只去掉重写时的default，不去掉接口中默认方法的default)；public可以省略，但default不可省略)
　　
　　(2)新增了”<u>接口中可以定义静态方法</u>”的特性，且接口中的静态方法不能被实现类重写，当实现类强行写一个同名静态方法时，也只是恰好多了一个方法，不影响这两个静态方法的分别调用
　　接口中静态方法的定义格式:

```java
public static 返回值类型 方法名(参数列表){}
```

(tip: 静态方法只能通过接口名调用，不能通过实现类名或者对象名调用；public可以省略，但static不可省略)

　　(3)新增了”<u>接口中可以定义私有方法</u>”的特性，当接口中有不想被其它地方调用的方法时(一般而言是借助私有方法来封装复用逻辑，例如接口中的某个方法仅为Inter接口提供服务，不需要外类访问时)，就可以定义为私有方法或静态私有方法
　　接口中私有方法的定义格式:

```java
private (static) 返回值类型 方法名(参数列表){}
```

(tip: <u>私有方法有两种，不加static为普通私有方法，服务于默认方法；加static则为静态私有方法，服务于静态方法(因为静态方法只能调用静态)；private不可省略</u>)
　　补充: 静态成员属于类，非静态成员属于实例，静态方法执行时没有绑定的实例对象，而在接口中，static方法属于接口本身，没有隐式的实例对象引用；且非静态方法内部隐含一个this关键字，指向当前调用方法的实例对象；而静态方法不属于类，不依赖实例，没有this引用，无法通过this去访问非静态的变量和方法。因此”静态方法只能调用静态成员”。

　　关于default和static关键字不能省略，是因为省略时Java会把它们当做是一个抽象方法，因为抽象方法的定义格式是:
　　<u>public abstract 返回值类型 方法名(参数列表);//无方法体</u>

  接口与接口之间是继承关系，可以单继承，也可以多继承，如果实现类实现了最下面的子接口的话，那么便需要重写所有的抽象方法

#### 三、接口的用法*

·声明接口:

```java
接口类型 j = new 实现类();//(或者使用集合存储+批量处理)
```

·方法接收任意接口类型实现类对象:

```java
[访问修饰符] [返回值类型] 方法名(接口类型 j){…}
```

　　二者的核心关联是都基于Java接口的多态特性，本质都是接口类型引用指向实现类对象，只是应用场景不同

​		接口与实现类之间存在一种非"传统类继承(extends)"关系的"实现(implements)"关系，而在多态和类型转换的语法层面，这种实现关系可以类比成"类与父类"的继承关系来理解，具体拆解如下：

##### (1)实现关系与继承关系的本质区别

​		类继承(extends)：是"is-a"的关系(如Dog extends Animal)，子类会继承父类的属性和方法，属于单继承(一个类只能继承一个父类)；
​		接口实现(implements)：是"can-do"的关系(如Mouse implements USB)，实现类仅需重写接口的抽象方法，不继承任何属性/方法，属于多实现(一个类可实现多个接口)。

##### (2)能类比成"继承关系"做向上转型

​		Java语法中，接口类型的引用可以指向其任意实现类的对象，这个规则和"父类引用指向子类对象"的向上转型完全一致，核心原因是：
​		1、实现类严格遵循了接口的"契约"：<u>重写了接口所有的抽象方法</u>，接口引用调用方法时，能确保实现类有对应的具体逻辑。
​		2、编译器认可这种类型兼容性：把实现类对象赋值给接口引用，编译器认为"这个对象具备接口定义的所有能力，可当做接口类型使用"。

##### (3)这种设计的意义

​		以Computer程序为案例，正因为实现类能向上转型为接口类型，才让Computer类的useUSB(USB usb)方法可以接收<u>任意USB实现类的对象</u>(Mouse、Keyboard，甚至后续新增的其UDisk类)，无需修改Computer的代码，这就是接口能够<u>**实现多态和解耦**</u>的核心体现。

---

### 14.耦合

​		耦合指的是软件模块之间的依赖程度，模块可以是类、函数、文件等代码单元

- 高耦合: 两个模块联系紧密，一个模块的修改会直接影响另一个模块，代码的维护性和扩展性
- 低耦合: 模块之间依赖弱，修改一个模块基本不会影响其他模块，是代码设计追求的目标
- 解耦: 降低模块间耦合度的过程，通过设计手段减少模块之间的直接依赖，比如使用接口、抽象类、设计模式等方法

---

### 15.内部类

#### 一、内部类概述

​		写在一个类里面的类就叫做内部类，当B类表示的事物是A类的一部分，其B单独存在没有意义。比如:汽车的发动机，ArrayList的迭代器，人的心脏等等

#### 二、内部类的分类

​		**成员内部类**：写在成员位置的，<u>属于外部类的成员</u>；成员内部类可以被一些修饰符所修饰，比如：private，默认，protected，public，static等，在成员内部类里面，JDK16之前不能定义静态变量，JDK16开始才可以定义静态变量(但一般成员内部类不使用static静态关键字修饰，否则就叫做静态内部类了)。获取成员内部类对象方法：
​		方式一：当成员内部类被private修饰时，在外部类中编写方法，对外提供内部类的对象，例如：

```java
public class Outer{
    private class Inner{
        int a = 666;   //内部类变量、方法等
    }
    public Inner creInstance{
        return new Inner();
    }
}
```

​		方法二：直接创建格式：外部类名.内部类名 对象名 = 外部类对象.内部类对象，例如：

```java
Outer.Inner oi = new Outer().new Inner();
```

​		

​		**静态内部类**：只能访问外部类中的静态变量和静态方法，若要访问外部类的非静态成员，需要先创建外部类的对象，再通过该对象去访问，例如

```java
public class Outer{		//外部类
    int a = 10;
    static int b = 20;
}
static class Inner{		//静态内部类
    public void show1(){	  //未创建外部类对象的方法
        System.out.println(a);//此处的a会报错
        System.out.println(b);
    }
    public void show2(){	  //创建了外部类对象的方法
        Outer o = bew Outer();//外部类对象
        System.out.println(o.a);//此处的a不会报错
        System.out.println(b);
    }
}
```

​		因为是静态的，因此创建静态内部类的对象时不需要先创建外部类的对象，直接用外部类的类名即可调用 (只要是静态的，都可以用类名直接获取)，格式为：

```java
Outer.Inner oi = new Outer.Inner();
```

​		对于调用静态内部类中的方法 (包含非静态方法和静态方法)：
​		调用非静态方法的格式：先创建静态内部类的对象，用对象调用
​		调用静态方法的格式：外部类名.内部类名.方法名(); (也可以先创建静态内部类的对象，用对象调用，但不推荐，静态可直接用类名获取，更推荐直接用"外部类名.内部类名.方法名()"调用)

​		**局部内部类**：将内部类定义在方法里面就叫做局部内部类 (因此局部内部类的前提是先定义一个方法)，类似于方法里面的局部变量 (因此可以修饰局部变量的也都能用来修饰局部内部类，例如final，同理，不能修饰局部变量的也不能用来修饰局部内部类，例如public/protected/default/private)。外界无法直接使用，需要在方法内部创建对象并使用。该类可以直接访问外部类的成员，也可以访问方法内的局部变量

**<u>		匿名内部类</u>**(需掌握)：本质上就是隐藏了名字的内部类，可以写在成员位置，也可以写在局部位置。严格来说，一般是创建匿名内部类的对象，格式是：

```java
new 类名或接口名(){
    @Override
	重写方法;
};
/*大括号及其所包含的内容才是匿名内部类，即不包含new和类名或接口名
事实上这个格式中包含了三个部分：
1、实现/继承关系——"接口名或类名"
2、方法的重写——"@Override……"
3、创建对象——"new"*/
```

​		理解：

```java
public interface Swim{	//游泳接口
    public abstract void Swim();//抽象类空方法体
}

public class Student implements Swim{//学生类实现游泳接口
    @Override		//重写接口抽象方法
    public void Swim(){
        System.out.println("学生游泳");
    }
}
/*在学生类当中，类名后面的大括号表示类的开始，类末尾的大括号表示类的结束，当把类名去掉后，剩余的内容就变成了一个没有名字的类，再把Swim写在大括号的前面，表示这个没有名字的类实现了Swim接口，所以需要在类中重写接口里面所有的抽象方法，如下：*/
Swim{
    @Override		//重写接口抽象方法
    public void Swim(){
        System.out.println("学生游泳");
    }
}
```

​		<u>匿名内部类的作用</u>：
​		匿名内部类是一种"即用即定义"的类，它把"定义子类/实现类"和"创建对象"这两个步骤合并成一步，而且这个临时的类没用名字，只用这一次，用完就不再复用，例如：

```java
//接口:
public interface Swim{	//游泳接口
    void swim();
}
//测试类:
Swim s = new Swim(){ //接口多态，创建接口的实现类对象
    @Override		//重写游泳接口的方法
    void swim(){
        System.out.println("学生游泳");
    }
};
s.Swim();	//接口类型的引用可以调用实现类重写的接口方法
//或者更简化的写法:
new Swim(){
    @Override
    void swim(){
        System.out.println("学生游泳");
    }
}.swim();
```

​		非静态内部类可以直接访问外部类的成员，包括私有
​		外部类要访问内部类的成员，必须创建对象，例如:

```java
public void show(Car this){
	System.out.println(this.carName);//外部类
	Engine e = new Engine();//创建一个内部类对象
	System.out.println(e.engineName);//这样才能访问
}
```

#### 三、内部类的内存图*

```java
public class Outer{
    private int a = 10;
    class Inner{
        private int a = 20;
        public void show(){
            int a = 30;
            System.out.println(Outer.this.a);
            System.out.println(this.a);
            System.out.println(a);
        }
    }
}   //内部类和外部类
```

```java
public class Test{
    public static void main(String[] args){
        Outer.Inner oi = new Outer().new Inner();
        oi.show();//测试类调用show()方法
    }
}
```

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2025-12-19 194049.png)
		首先，类(Test.class测试类、Outer.class外部类和Outer$Inner.class内部类)在加载时，字节码文件中的类元数据(类结构、方法信息等)会被加载到方法区(JDK 8及以后由元空间实现)，需要注意的是，外部类和内部类在内存当中是两个独立的字节码文件。在堆内存中，内部类对象并不会像在代码结构中那样嵌套在外部类对象当中，而是独立于外部类存在，但在内部类对象的空间中，会有隐藏一个Outer.this语法访问表达式，其中的Outer是外部类名，this则代表当前外部类实例的引用，即内部类中实际隐藏的是类型为Outer的成员变量(底层命名一般是this$0，用于和指向内部类的this区分开)，而Outer.this是对该隐藏变量的语法访问方式，用来记录外部类对象的地址值。oi变量中存储的是内底部类对象的地址值，因此当执行oi.show()时，方法中的this指向的调用者也是内部类对象，因此：
		当执行sout(a)时，触发就近原则 (<u>本质是变量的作用域优先级：局部变量>内部类实例变量>外部类实例变量</u>) 输出方法内定义的a；
		当执行sout(this.a)时，输出内部类实例(this)的成员变量a；
		当执行sout(Outer.this.a)时，会<u>访问Outer.this所指向的外部类实例</u>中的成员变量a (类成员是静态的，这里是实例成员) ，因此，当外部类成员变量和内部类成员变量重名时，在内部类则可以通过"Outer.this.变量名"进行访问

***

### 16.GUI图形化界面(略)

​		Graphical User Interface又称图形用户接口，即通过图像化的方式显示操作界面，在Java中有两套完整的体系，第一套体系是定义在AWT包下的，第二套体系则是定义在Swing包下的

​		AWT包：早期GUI，可能存在兼容性问题，中文可能显示乱码，现在不推荐使用；
​		Swing包：更推荐使用

​		Java主要做的是后台服务器开发，很少会直接和用户接触，因此和图形化界面相关的知识点在企业当中很少会有人去用，但利用GUI去尝试写一些小的项目，能够为新手提供一个知识点整合期的过渡，同时激发一些兴趣，能够<u>锻炼逻辑思维能力</u>，<u>知道前面学习的知识点在实际开发中的应用场景</u>

***

### 17.服务器(略)

​		通过服务器把内容传递过来并展示在浏览器中，这一系列业务逻辑诸如登录、订阅等功能都是服务器在操作。在一个项目当中，用户是接触不到服务器的，只能接触到客户端，这个客户端要么是浏览器，要么是用C或C++写的一个客户端的安装包，用户需要下载和安装，要么是在手机当中写了一个APP等

​		输入一个网址>转换成DNS

***

### 18.项目介绍和界面搭建

#### 一、主界面(UI)

​		组件类型：
​		**JFrame** (即Java Frame) —— 最外层的窗体，也就是界面，一般而言项目内容都需要写在窗体当中以供展示。可以将其理解为一个Javabean类，用来描述界面，其中有很多属性 (例如宽和高) 和行为 (例如setSize、setVisible等)，例如：

```java
public class GameJFrame extends JFrame {//继承JFrame类
    //规定为游戏主界面，以后游戏中的所有相关逻辑都写在这个类当中
    public GameJFrame(){
        //设置界面宽高
        this.setSize(200,300);
        //设置界面的标题
        this.setTitle("示例界面");
        //设置界面置顶
        this.setAlwaysOnTop(true);
        //设置界面居中
        this.setLocationRelativeTo(null);
        //设置界面关闭模式
	    this.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        //让界面显示/隐藏
        this.setVisible(true);
    }
}
```

​		**JMenuBar** (即Java Menu Bar) —— 最上层的菜单，创建顺序：

<img src="C:\Users\22345\Pictures\Screenshots\屏幕截图 2025-12-21 021601.png" style="zoom: 80%;" />

​		然后会有这么一个过程：先创建全部的对象，再按顺序将JMenuItem放到JMenu里，再将JMenu放到JMenuBar里，再将JMenuBar添加到整个JFrame界面中：

```java
//创建整个的菜单对象
JMenuBar JmenuBar = new JMenuBar();
//编写具体选项(JMenu)、条目(JMenuItem)
……
//将每个条目对象添加到对应的选项对象当中
functionJMenu.add(replayItem);
functionJMenu.add(reLoginItem);
functionJMenu.add(closeItem);
aboutJMenu.add(accountItem);
//将每个选项对象添加到对应的菜单对象当中
JmenuBar.add(functionJMenu);
JmenuBar.add(aboutJMenu);
//给整个界面设置菜单
this.setJMenuBar(JmenuBar);
```

​		而条目对象内的能够执行具体操作事件的则是 JMenuItem，如果在菜单中还需要嵌套二级的菜单，那么还可以用JMenu完成，JMenu里面是可以再次添加其他的JMenu的

​		**JLabel** (即Java Label) —— 管理文字和图片的容器，在管理图片的时候，可以对图片的宽高、位置还有边框进行设置。代码实现：可以先分别创建一个ImageIcon对象和一个JLabel对象，指定图片在电脑中的位置，然后把图片交给JLabel，再把整体放到界面当中，默认放在界面正中央

#### 二、事件

​		**事件源**：可被操作的事物，如按钮、图片、窗体……
​		(1) **JButton** 按钮，一般创建在<u>界面</u>模块，其对象的创建格式为：

```java
JButton jbt = new JButton("Click it.");//有参构造
//设置位置和宽高
jbt.setBounds(0,0,100,50);
//给按钮添加动作监听(鼠标左键点击、空格)事件
jbt.addActionListener();//括号中的参数表示事件被触发之后要执行的代码，且该参数是一个名字叫作ActionListener的接口类型，所以该参数应该填写一个该接口的实现类对象。因此可以先创建一个Method实现类并重写接口方法从而实现ActionListener接口，再在参数中用new Method()创建该实现类对象传参
```

​		**事件**：某些操作，如鼠标单击，鼠标划入，键盘操作……

​		**绑定监听**：当事件源上发生了某个事件，则执行某段代码，常见的监听有：
​		(1) **KeyListener** 键盘监听，包括快捷键的实现等
​		(2) **MouseListener** 鼠标监听，能监听鼠标按下不松和松开等操作
​		(3) **ActionListener** 动作监听，可以理解为键盘监听和鼠标监听的精简版，监听鼠标的时候只能监听点击，监听键盘的时候只能监听空格

***

### 19.爬虫

#### 一、本地爬虫

​		1.获取正则表达式的对象。不可直接new创建，要通过compile()方法创建：

```java
Pattern p = Pattern.compile("regex正则表达式");  //方法返回一个Pattern对象
```

​		2.获取文本匹配器的对象。用创建好的正则表达式的对象调用matcher()方法，并将要爬取的大串内容传递至方法的参数列表当中，而方法则会返回一个文本匹配器的对象：

```java
Matcher m = p.matcher(str);  //m即文本匹配器对象，str即大串，p即规则。理解：文本匹配器m要在字符大串str中查找符合规则p的小串。
```

​		3.用文本匹配器m对象调用find()方法，能够返回查找结果：如果没有，方法返回false；如果有，返回true，在底层记录子串的起始索引和结束索引+1(因为group方法的底层实现运用到了subString方法截取，而subString方法具有包头不包尾的特点，因此结束索引需要+1):

```java
boolean b = m.find();  //返回值是boolean类型，如果有则是在底层记录索引，需要调用group()方法才能读取索引并获取字符串
```

​		4.用文本匹配器m对象调用group()方法，方法底层会根据find()方法记录的索引进行字符串的截取，并把截取的小串进行返回:

```java
String s1 = m.group();
```

​		5.当第二次调用find的时候，会继续读取后面的内容，因此可以使用循环:

```java
//最终代码
//1.获取正则表达式对象
Pattern p = Pattern.compile("正则表达式");
//2.获取文本匹配器的对象
Matcher m = p.matcher(str);
//3.利用While循环获取
while(m.find()){  //如果未找到目标会返回false自动停止循环
    String s = m.group();
}
```



#### 二、网络爬虫

​		1.创建一个URL对象 (可以理解为一个网址的对象):

```java
URL ur1 = new URL("https://……");  //输入需要爬取的网站的网址
```

​		2.连接上这个网址 (tip:要保证网络是否畅通):

```java
URLConnection conn = ur1.openConnection();
```

​		3.创建一个对象去读取网络中的数据 (读取的时候每次读一整行):

```java
BufferedReader br = new BuffereReader(new InputStreamReader(conn.getInputStream()));
String line;

while((line = br.readLine()) != null){  //循环每行
    Matcher matcher = pattern.matcher(line);  
    while(matcher.find()){  //查找当前行是否有匹配条件的小串
        System.out.println(matcher.group());
    }
}
br.close();
```

#### 三、正则表达式进阶

​		**1.(?i)忽略大小写	(?=)跟随数据**

```java
String regex = "((?i)Java)(?=8|11|17)";  
//(?i)表示忽略大小写
//?占位，理解为前面的整体数据((?i)Java)
//=表示在Java后面要跟随的数据
//但是在获取时只获取前半部分(如果将=改为:则是获取全部，如果改为!则是不获取含有后面跟随的数据的内容)
```

​		**2.贪婪爬取(默认)	非贪婪爬取**

```java
String s = "abbbbbbbbb";
String regex1 = "ab+";  //贪婪爬取(默认)，获取abbbbbbbbb
String regex2 = "ab+?";  //非贪婪爬取，获取ab
```

​		**3.按照正则表达式的规则进行替换**

```java
String s = "fsefsdvrg";
String result = s.replaceAll(regex,replacement);  //表示将字符串s中满足regex的部分全部替换成replacement并输出字符串类型的替换结果。方法在底层跟之前一样也会创建文本解析器的对象，然后从头开始去读取字符串中的内容，只要有满足的，那么就用第二个参数去替换
```

​		**4.按照正则表达式的规则切割字符串**

``` java
String[] arr = s.split("regex");  //表示在s字符串中切割regex指定的字符子串并以字符串数组的形式返回切割后的结果，如6421regex1542regex32切割为{"6421","1542","32"}
```

​		**5.组**

​		以左括号为基准，最左边的是第一组，其次为第二组，以此类推，每组都有组号，也就是序号，从1开始，连续不间断

​		捕获分组 (后续还要继续使用本组的数据):

```java
String regex = "(.).+\\1";  //其中的\\n表示调用第n组数据，这种调用叫作捕获分组
```

​		口吃替换:

```java
String s = "我要学学编编编程程程程程";  //把重复的内容替换为单个的
s.replaceAll("(.)\\1+","$1");  //前者表示需要被替换的部分的正则表达式regex，后者表示替换的内容replacement

//tip:如果是在正则表达式内部使用组，则用\\n，如果是在正则表达式外部使用，那么用$n
```

​		非捕获分组 (分组之后不需要再用本组数据，仅仅是把数据括起来): 

```java
String regex = "(?:.)\\1";  //这里的?:表示非捕获分组，不占用组号，同样的还有?=、?!也表示非捕获分组
```



### 20.时间 

#### 		(Date + SimpleDateFormat + Calendar) (白雪系列，JDK7以前的老版本，多线程环境下会导致数据安全问题)

​		Date类指时间:

```java
Date date = new date(可填毫秒值);
date.setTime(long);  //设置时间
date.getTime();  //获取时间毫秒值
```

​		SimpleDateFormat类作用：
(1) **格式化**：把时间变成其他格式，例如 2023年1月1日；
(2) **解析**：把字符串表示的时间变成Date对象。

```java
//构造方法
public SimpleDateFormat()  //构造一个对象，使用默认格式:1970/1/1 上午8:00
public SimpleDateFormat(String pattern)  //使用指定的格式
//常用方法
public final String format(Date date)  //格式化(日期对象 -> 字符串)
    //String str = sdf.format(date);返回值是String形式
public Date parse(String source)  //解析(字符串 -> 日期对象)
    //Date date = sdf.parse(str);返回值是Date形式
//格式化的时间形式的常用的模式对应关系如下:
//y年 M月 d日 H时 m分 s秒
//例如: yyyy-MM-dd HH:mm:ss
```

​		Calendar：代表了系统当前时间的日历对象，可以单独修改、获取时间中的年，月，日等。并且Calendar是一个抽象类，不能直接创建对象，需要通过静态方法获取当前时间的日历对象
​		底层原理：会根据系统的不同时区来获取不同的日历对象，默认表示当前时间，把时间中的纪元，年，月，日，时，分，秒，星期等等都放到一个数组当中。此处有一个细节，当获取月份时，范围是0~11，需要在获取出来的基础上+1；而当获取星期时，星期日是一周中的第一天，因此返回1时表示星期日，以此类推

```java
//获取对象(抽象类无实例对象，无构造方法)
public static Calendar getInstance()  //获取当前时间的日历对象
//Calendar常用方法
public final Date getTime()  //获取日期对象
public final setTime(Date date)  //给日历设置日期对象

//Calendar重点方法
public int get(int field)  //取日历中的某个字段信息，在参数列表中输入数组中对应索引的值或者名称
    //int year = c.get(1或者YEAR);
    //int month = c.get(2或者MONTH)+1;  //月份需要+1
    //int date = c.get(5或者DAY或者DATE_OF_MONTH);
    //int week = c.get(7或者DAY_OF_WEEK);
public void set(int field,int value)  //修改日历的某个字段信息
    //c.set(Calendar.TEAR,2000);
    //c.set(Calendar.MONTH,12-1);  //12月，月份需要-1
public void add(int field,int amount)  //为某个字段增加/减少指定的值
    //c.add(Calendar.MONTH,-1);  //表示往前减一个月
```

#### 		(JDK8以后，时间日期对象都是不可变的，修改时会产生一个新的时间对象)



### 21.包装类

​		概述：基本数据类型对应的引用数据类型，例如int对应的Integer。在Java当中，万物皆对象，并且由于多态的存在，所有的对象都可以用Object来进行表示，而如果没有包装类，程序接收不了基本数据类型时，程序就会有局限性。而在集合中就不能存储基本数据类型，只能存对象

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-16 220514.png)

​		获取方式：以Integer为例，利用构造方法和静态方法获取的Integer对象是不相同的。构造方法new创建的对象是一定会创建一个对象的，而当使用静态方法获取时-128~127范围内的int数据时会从底层源码的一个数组中获取。

​		计算方式：对象是不能直接进行计算的。一般需要先进行拆箱，变成基本数据类型计算，然后再进行装箱。但对于包装类来说这样的步骤太过繁琐，因此在JDK5以后提出了一个新特性：自动装箱 (把基本数据类型自动变成其对应的包装类) 和自动拆箱 (把包装类自动变成其对象的基本数据类型) 。

```java
//自动装箱
Integer i1 = 10;  //可以直接这样写，底层会自动调用valueOf得到一个Integer对象
//自动拆箱
int i2 = i1;
```

(tip: 以后在进行键盘录入的时候，使用String类型接收，统一使用sc.nextLine()方法录入，能够录入一整行的数据，特点是遇到回车才停止。而当后续想要将录入的数据进行格式转化时再利用包装类的静态转化方法即可：

```java
Scanner sc = new Scanner(System.in);
String line = sc.nextLine();
//需要将录入的数据转化成int数据类型时：
int i = Integer.parseInt(line);
//转化成double类型时:
double d = Double.parseDouble(line);
```



### 22.集合

#### (一)集合体系结构

​		在Java当中有很多种集合，主要可以分为两类。第一类集合叫作单列集合，第二类集合叫作双列集合。

​		单列集合在添加数据的时候每次只能添加一个元素，它最顶层的是Collection，其下面又可以分为两种集合， List 和 Set，List 集合的下面又可以分为 ArrayList，LinkedList 和 Vector (已淘汰)；Set 集合的下面则可以分为 HashSet 和 TreeSet，HashSet的下方则是 LinkedHashSet

***

#### (二)单列集合Collection

​		**<u>Collection</u>**：是集合框架的顶层根接口之一，也是单列集合的祖宗接口，它的功能是全部单列集合都可以继承使用的。因此存储的是一些共性的方法
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-29 100715.png)
​		remove(E e)方法是根据对象进行删除的，因为Collection中定义的是共性的方法，而其实现类Set系列集合是没有索引的，因此无法通过索引进行删除
​		contains(Object obj)方法在底层是依赖equals方法和循环遍历进行判断是否存在的，所以如果集合中存储的是自定义对象，也想通过contains方法来判断是否包含，那么在自定义的Javabean类中一定要重写equals方法。此处**关于重写方法可以通过Alt+Insert快捷键进行代码生成，记得删去hashCode方法的重写部分**，暂时还用不上
​		isEmpty()方法在底层的执行其实就是判断集合的长度是否为0

​		因为Collection本质上是一个接口，因此我们不能直接创建它的对象，在学习其方法时只能创建它的实现类的对象

------

​		**<u>List</u>**系列集合：添加的元素是**有序**、**可重复**、**有索引**
​		1.**有序**：指的是存和取的顺序是一样的
​		2.**可重复**：表示集合中存储的元素是可以重复的，基于这一点，在往List系列的集合中添加数据时，结果永远返回true
​		3.**有索引**：即指可以通过索引去获取集合里面的每一个元素

​		List集合因为有索引，所以多了很多索引操作的方法：
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-31 174656.png)
​		tip：当集合的泛型为Integer，remove方法的实参填的也是Int类型时，会优先执行索引删除的方法，因为当方法重载时，会优先调用实际参数跟形参保持一致的那个方法。而如果想要使用Collection中的remove方法，则需要对数据进行手动装箱，例如：

```java
List<Integer> list = new ArrayList<>();
list.add(1);
Integer i = Interger.valueOf(1);  //手动装箱，将基本数据类型中的1变为Integer类型
list.remove(i);  //优先调用实参和形参保持一致的Collection方法
```

​		列表迭代器相较于迭代器多出的方法：	

```Java
ListIterator<String> it = list.listIterator();  //获取一个列表迭代器的对象
while(it.hasNext()){
    String s = it.next();
    it.add("abc");  //用列表迭代器提供的add方法可以添加元素
}
```

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-31 202350.png)

​		**List**下分的三个实现类：**ArrayList** / **LinkedList** /  **Vector**
​		见名知意。ArrayList即底层数据结构为Array (数组) 的集合，具有数组的**内存连续区域，查询快，增删慢**的特点；而LinkedList即底层数据结构为Linked (链表) 的集合，具有链表的**元素是游离的，查询慢，首尾操作极快**的特点。根据这两种List系列的实现类各自的特点，在List系列集合本身所含有的方法的基础上又各自增加了许多方法，例如依据LinkedList链表数据结构的特点，新增了专门用于首尾增删的数据的方法。

***

​		<u>**Set**</u>系列集合：添加的元素是**无序**、**不重复**、**无索引**
​		1.**无序**：指的是存和取的顺序有可能是不一样的
​		2.**不重复**：表示集合中存储的元素是不可以重复的，基于这一点，可以利用Set集合的这个特性进行数据的去重
​		3.**无索引**：即指无法通过索引去获取集合里面的每一个元素

***



![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-29 095350.png)
		**Collection的遍历方式**(因为Collection接口的子接口Set是无索引的，因此没有普通for循环遍历的方式)：
		**1.迭代器遍历**：迭代器**不依赖索引**，在Java中的类是Iterator，迭代器是集合专用的遍历方式。可以通过Iterator的一个方法去获取迭代器的对象：
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-29 124148.png)

​		![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-29 124434.png)

​		然后利用迭代器中的方法拼接while循环去获取到集合里面的每一个元素：
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-29 125810.png)

​		迭代器的next()方法可以获取迭代器指向的元素并返回该元素，然后让迭代器指向下一个元素，因此需要用同类对象去接收方法返回值。而当迭代器指向了没有元素的位置还强行用next方法去获取元素就会报错NoSuchElementException，之所以不会报错索引越界，是因为迭代器是不依赖索引的；之所以只能使用一次next方法是因为当使用n次时，如果集合中的元素数量不是n的倍数，那么会触发NoSuchElementException报错；之所以遍历时不能使用集合的方法进行增删是源于Java集合框架中的一种名为 **fail-fast (快速失败)** 的故障检测机制 (核心目的是**在集合遍历过程中快速发现非预期的集合修改并抛出异常**，避免程序在数据不一致的状态下继续执行，从而防止更隐蔽的逻辑错误、数据错乱，是一种"**宁错停，不隐错**"的设计思想) ：迭代器遍历会依赖集合的修改次数标记 (modCount) ，遍历前迭代器会记录该标记，若遍历中直接使用集合的增删方法 (add和remove) 修改集合就会导致modCount变化，迭代器就会检测到标记不一样，就会抛出**ConcurrentModificationException并发修改异常**，也就是说，迭代器和集合是"两套操作入口"，直接改集合会让迭代器的遍历状态失效，触发异常保护。因此总结：为避免并发修改异常，在使用迭代器或是增强for遍历集合的过程中，不要使用集合的方法去添加或者删除元素即可。
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-29 131023.png)		
​		**2.增强for遍历**：在JDK 5之后出现，增强for的底层就是迭代器，其内部原理就是一个Iterator迭代器，为了简化迭代器的代码书写的。所有的单列集合和数组才能用增强for进行遍历，而双列集合则不能。格式：

```Java
for(元素的数据类型 变量名 : 数组或者集合){
    ……
}
//例如：
for(String s : coll){  //这里的s只是一个第三方变量
	System.out.println(s);
}
//快捷键coll.for即可，注意不是coll.fori和coll.forr
```

​		**3.Lambda表达式遍历**：得益于JDK 8开始的新技术Lambda表达式，提供了一种更简单、更直接的遍历集合的方式。

```java
default void forEach(Consumer<? super T> action):  //结合lambda遍历集合
//在实际使用的过程中，只需要这样写即可：
集合名.forEach(i -> {  //注意：这里的i表示
		方法体    
	}
);
//而当方法体只有一行的时候则可以省略大括号，例如：
coll.forEach(i -> i.sout);
```

​		**总结：**当遍历的过程中需要删除元素时，则使用迭代器；而仅仅想遍历，那么使用增强for或Lambda表达式。

#### (三)双列集合

​		双列集合在添加数据的时候每次添加一对数据，即两个数据。

#### (四)数据结构

​		**数据结构概述**：

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-31 203817.png)

​		**常见的数据结构**：

​		**1.栈**
​		栈的特点：**后进先出，先进后出**
​		可以把栈看成一个容器，开口端叫作栈顶，封闭端叫作栈底。数据进入栈模型的过程称为压/进栈，数据离开栈模型的过程称为弹/出栈。最后进来的在最上面，称为栈顶元素，最先进来的在最下面，称为栈底元素。

​		**2.队列**
​		队列的特点：**先进先出，后进后出**
​		可以把队列看成一个通道，数据从后端进入队列模型的过程称为入队列，数据从前端离开队列模型的过程称为出队列。

​		**3.数组**
​		数组的特点：**内存连续区域，查询快，增删慢**
​		查询数据通过地址值和索引定位，查询任意数据耗时相同 (元素在内存中是连续存储的)；要将原始数据删除，同时后面每个数据前移；添加位置后的每个数据后移，再添加元素。

​		**4.链表**
​		链表的特点：**元素是游离的，查询慢，首尾操作极快**
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-31 214530.png)

​		链表的元素称为结点，每个结点的存储位置都是一个地址，结点中包含了该结点存储的具体数据和下一个结点的地址值，所有的结点都需要像一根链子一样组成一条链表。链表当中第一个被创建出来的结点称为头结点head，其中也有自己的数据；最后一个结点用尖角符号^表示，这个结点的后面没有其他节点了，记录的是空地址。因此创建一个链表的过程是：创建一个头结点，再创建一个结点，将后创建的结点的地址值填入前一个结点存储的地址值空间中，就可以实现结点之间的链接，实现链表。
​		**链表中的结点是独立的对象，在内存中是不连续的，每个结点包含数据值和下一个结点的地址，链表查询慢，无论查询哪个数据都要从头开始找。**
​		链表有两种形式，单向和双向，双向链表可以提高查询效率，既可以从前往后找，也可以从后往前找
![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-31 220129.png)

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-31 215400.png)

​		**5.二叉树**

​		**6.二叉查找树**

​		**7.平衡二叉树**



### 23.泛型

​		泛型：是JDK 5中引入的特性，可以在编译阶段进行类型检查和约束的机制。
​		泛型的核心作用：
​		**1.类型安全**
​		**2.代码重用**
​		**3.消除强转**

​		泛型的格式：<数据类型>
​		注意：泛型只能支持引用数据类型，也就是不能写int而必须写Integer。如果没有给集合指定类型，默认认为所有的数据类型都是Object类型，此时可以往集合添加任意的数据类型；而指定泛型的具体类型后，传递数据时，可以传入该类类型或者其子类类型

```java
//创建集合对象
ArrayList list = new ArrayList();  //无泛型
//添加数据
list.add(123);
list.add("aaa");
list.add(new Student("zhangsan",18));  //有学生类的情况下
//坏处：获取数据的时候，无法使用他的特有行为

//传递类或者子类类型对象(一般而言为了保证数据类型一致不会传递子类对象)
ArrayList<Animal> list = new ArrayList<>();
list.add(new Animal());
list.add(new Cat());
list.add(new Dog());

//tip:迭代器也可以添加泛型
Iterator<String> it = list.iterator();  //此时使用迭代器的方法也不需要再强转了
```

​		因此Java推出了泛型，目的是可以在添加数据的时候就把类型进行进行了统一，而且我们在获取数据的时候，也无需强转

​		扩展知识点：Java中的泛型是伪泛型。Java泛型只在编译期有效，运行时会被"擦除"，本质上还是用原生类型 (Object) 实现的，没有真正在JVM层面生成不同的字节码类型，例如List<String>在运行时就是个普通的List，类型擦除的核心作用是**为了兼容JDK 5之前的版本**

​		泛型可以在很多地方进行定义，在类后面则此类称为泛型类，在方法上面则此方法被称为泛型方法，在接口后面则此接口被称为泛型接口。
​		泛型类：当一个类中，某个变量的数据类型不确定时，就可以定义带有泛型的类

```java
修饰符 class 类名<类型>{
    ……
}
//例如:
public class ArrayList<E>{
    ……
}
//此处E可以理解为变量，但是不是用来记录数据的，而是记录数据的类型，可以写成:T、E、K、V等
```

​		泛型方法：当类中不同方法需要用到不同数据类型或者只有一个方法需要用到泛型时，则可以在方法的修饰符和形参中添加泛型

```java
//类中定义一个静态方法addAll，用来添加多个集合的元素
public static<E> void addAll(ArrayList<E> list,E...e){
    for(E element : e){
        list.add(element);
    }
}
```





# JavaWeb

### Web基础

#### 1.Web应用

​		由静态资源（HTML/CSS/JS/图片）+ 动态资源（Java 程序/Servlet/JSP）组成，运行在服务器上，供客户端浏览器访问。

#### 2.B/S架构

​		浏览器（Browser）-服务器（Server），跨平台，维护简单（仅需维护服务器），JavaWeb 核心架构。

#### 3.URL

​		统一资源定位符，格式为：协议://域名:端口/资源路径?参数

#### 4.HTTP协议

​		**定义**

​		超文本传输协议，是**Web客户端（浏览器）和服务器之间通信的"语言"**，定义了请求/响应的格式和交互方式，是JavaWeb应用的通信基础。HTTP是无状态的，即服务器不记得之前的请求者。这意味着每次请求都是独立的，需要通过Cookie (客户端存储) 、Session (服务器存储) 解决无状态

​		**核心要素**
​		请求报文 (请求行+请求头+空行+请求体)
​		相应报文 (状态行+响应头+空行+响应体)
​		常用请求方法 (GET/POST)
​		状态码 (200/302/404/500)

#### 5.静态/动态资源区分

​		静态资源：服务器无需处理，直接返回给客户端，内容固定 (HTML/CSS/JS/图片)
​		动态资源：服务器需通过 Java 程序处理后，动态生成内容返回，内容随参数/数据变化 (如查询数据库后的页面)

#### 6.JSP

​		**定义**

​		Java Server Pages，本质上是Servlet的一种简化形式，允许在HTML中嵌入Java代码，用于快速生成动态网页，最终会被服务器编译为Servlet执行。

​		**核心特性**

​		本质：Servlet (JSP = HTML + Java)，简化动态页面开发 (无需手动写 Servlet 输出 HTML)
​		页面组成：静态 HTML + JSP 脚本 + JSP 指令 + JSP 动作标签

​		**核心语法**

​		JSP 脚本
​		JSP 指令
​		JSP 内置对象

​		**JSP四大域对象**

​		pageContext：页面域，仅当前 JSP 页面有效 (跳转后失效)
​		request：请求域，当前请求有效 (请求转发共享，重定向失效)
​		session：会话域，当前用户会话有效 (浏览器关闭前均有效)
​		application：应用域，整个 Web 应用有效 (服务器运行期间，所有用户共享)

#### 7.MVC模式

​		核心设计思想，是一种软件设计模式，将应用分为三层：

​		**Model (模型)**

​		负责业务逻辑和数据交互，包括实体类 (POJO/Bean) (封装数据，如 User、Student) 和业务层    (Service) (处理核心业务，如登录验证、数据查询)、数据访问层 (DAO) (操作数据库，如 UserDAO、StudentDAO)

​		**View (视图)**

​		负责页面展示，即 JSP/HTML 页面，仅做数据展示，无业务逻辑

​		**Controller (控制器)**

​		负责请求处理，即 Servlet，接收客户端请求，调用 Service 层处理业务，将处理结果传递给 View 层展示

---

### 核心组件

#### 1.Servlet

​		**定义**

​		运行在服务器端的Java程序，用于接收客户端HTTP请求并生成动态响应，是JavaWeb的核心组件，所有Web请求最终都由Servlet处理。

​		**核心特性**

​		由服务器 (Tomcat) 创建和销毁，遵循生命周期，程序员仅需重写业务方法
​		单例多线程：一个Servlet类仅创建一个实例，处理多个客户端请求 (注意线程安全，避免定义成员变量)

​		**生命周期**

​		初始化：服务器启动/首次访问时，调用 init() ，仅执行一次
​		处理请求：每次客户端请求，调用 service() ，自动根据请求方法分发到 doGet() / doPost()
​		销毁：服务器关闭时，调用 destroy() ，仅执行一次，释放资源

​		**核心使用**

​		自定义Servlet：继承 HttpServlet ，重写 doGet() / doPost() (无需重写service())
​		配置方式：(1)注解@WebServlet("/路径") (主流方式，能够简化配置)；(2)web.xml配置 (传统方式)

​		**核心对象**

​		HttpServletRequest (请求对象，获取请求参数/头/会话)
​		HttpServletResponse (响应对象，向客户端返回数据/页面/重定向)

#### 2.Filter过滤器

​		**定义**

​		运行在服务器、在请求到达Servlet之前或响应返回客户端之前对其进行拦截和处理的组件，可对请求/响应进行预处理/后处理。

​		**核心特性**

​		拦截范围：可拦截 Servlet、JSP、HTML 等所有资源
​		生命周期：服务器启动时创建 init() → 每次拦截时执行 doFilter(req,resp,chain) → 服务器关闭时销毁 destroy()

​		**核心使用**

​		自定义 Filter：实现 Filter 接口，重写 init() / doFilter() / destroy() ，核心在 doFilter() 中写拦截逻辑
​		放行： chain.doFilter(req,resp) （必须调用，否则请求无法到达目标资源）
​		配置方式：(1)注解@WebFilter("拦截路径") ；(2)web.xml配置
​		拦截路径： /* (拦截所有资源，最常用)、 /servlet/* (拦截指定路径下的资源)、 *.jsp (拦截指定后缀的资源)

#### 3.Listener监听器

​		**定义**

​		监听Web应用中特定事件（如ServletContext创建销毁、Session创建销毁、属性变更）的组件，可在事件触发时执行自定义逻辑，如初始化全局数据、统计在线人数等

 		**常用监听器**

​		ServletContextListener：监听 ServletContext 的创建/销毁，服务器启动时初始化全局数据 (如加载配置文件、连接数据库)
​		HttpSessionListener：监听 Session 的创建/销毁，统计在线人数 (每次创建 Session 表示一个用户上线，销毁表示下线)

​		**配置方式**

​		(1)注解@WebListener (主流)；(2)web.xml配置

---

### 关键技术

#### 1.Cookie

​		存储在客户端的小型文本数据，由服务器生成并发送给浏览器，浏览器会在后续请求中自动携带，用于实现会话跟踪（如记住登录状态），但存储容量有限且安全性较低

#### 2.Session

​		存储在服务器端的会话数据，通过唯一的Session ID与客户端关联，用于在多个请求之间保持用户状态（如购物车、登录信息），比Cookie更安全且容量更大

#### 3.Request对象

​		代表客户端的HTTP请求，封装了请求头、请求参数、请求方法等信息，Servlet通过该对象获取客户端提交的数据

#### 4.Response对象

​		代表服务器的HTTP响应，用于向客户端发送数据 (如HTML、JSON)、设置响应头 (如Content-Type) 和状态码 (如404、500)

---

### 开发工具

#### 1.Maven

​		项目管理和构建工具，通过 pom.xml 文件管理项目依赖、构建流程和项目信息，可自动下载第三方库 (如Servlet API)，简化项目配置和构建过程。

#### 2.IDEA中Web项目配置：

(1)创建Web项目时选择Java Enterprise或Web模板

(2)配置Tomcat服务器作为运行环境

(3)配置 web.xml  (可选，Servlet 3.0+可通过注解配置)

(4)管理依赖 (通过Maven或手动导入Jar包)

(5)部署和运行Web应用





+++

# MySQL

### 1.MySQL概述

#### 		(1)数据库相关概念

​		数据库：存储数据的仓库，数据是有组织地进行存储，DateBase (DB)
​		数据库管理系统：操纵和管理数据库的大型软件，DataBase Management System (DBMS)
​		SQL：操作关系型数据库 (RDBMS) 的编程语言，定义了一套操作关系型数据库统一**标准**，Structured Query Language (SQL)

​		三者关联：可以通过SQL来操作数据库管理系统，再利用数据库管理系统来操作数据库

#### 		(2)MySQL数据库

​		MySQL数据库属于关系型数据库，是建立在关系模型基础上，由多张相互连接的二维表组成的数据库。特点：1.使用表存储数据，格式统一，便于维护；2.使用SQL语言操作，标准统一，使用方便

### 2.SQL

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-21 051120.png)

#### (一)SQL分类(DDL/DML/DQL/DCL)

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-21 051338.png)

#### (二)DDL

- **DDL-介绍**

  ​	略。

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-21 051824.png)

- **DDL-表查询-查询**

  ```sql
  SHOW TABLES;  --查询当前数据库所有表，前提是需要先通过USE指令先进入到这个数据库
  DESC 表名;  --查询表结构
  SHOW CREATE TABLE 表名;  --查询指定表的建表语句
  ```

- **DDL-表操作-创建**

  ```sql
  CREATE TABLE 表名(
  	字段1 字段1类型[COMMENT 字段1注解],
      字段2 字段2类型[COMMENT 字段2注解],
      字段3 字段3类型[COMMENT 字段3注解],
      ……
      字段n 字段n类型[COMMENT 字段n注解]
  )[COMMENT 表注解];
  ```


- **DDL-表操作-数据类型**

  数值类型：

  ![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-21 144057.png)

  字符串类型：

  ![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-21 144611.png)

  日期类型：

  ![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-01-21 144907.png)

- **DDL-表操作-修改 (表内字段)**

  ```sql
  ALTER TABLE 表名 ADD 字段名 类型(长度) [COMMENT 注解] [约束];  --添加字段
  ALTER TABLE 表名 DROP 字段名;  --删除字段
  ALTER TABLE 表名 MODIFY 字段名 新数据类型(长度);  --修改数据类型
  ALTER TABLE 表名 CHANGE 旧字段名 新字段名 类型(长度) [COMMENT 注释] [约束];  --修改字段名和字段类型
  
  ALTER TABLE 表名 RENAME TO 新表名;  --修改表名
  ```

- **DDL-表操作-删除**

  ```sql
  DROP TABLE [IF EXISTS] 表名;  ---删除表
  TRUNCATE TABLE 表名;  --删除指定表，并重新创建该表
  
  /*需要注意的是，在删除表时，表中的全部数据也会被删除*/
  ```

#### (三)DML

- **DML-介绍**

  ​		DML英文全称是Data Manipulation Language (数据操作语言) ，用来对数据库中表的数据记录进行增删改操作。关键字：
  ​		1.**添加数据-INSERT**
  ​		2.**修改数据-UPDATE**
  ​		3.**删除数据-DELETE**

- **DML-添加数据**

```sql
INSERT INTO 表名 (字段名1，字段名2，……) VALUES (值1，值2，……);  --给指定字段添加数据
INSERT INTO 表名 VALUES (值1，值2，……);  --给全部字段添加数据
INSERT INTO 表名 (字段名1，字段名2，……) VALUES (值1，值2，……),(值1，值2，……);
INSERT INTO 表名 VALUES (值1，值2，……),(值1，值2，……);  --批量添加数据
```

- **DML-修改数据**

```sql
UPDATE 表名 SET 字段名1=值1,字段2=值2,……[WHERE 条件];  --注意:修改语句的条件可以有，也可以没有，如果没有条件，则会修改整张表的所有数据
```

- **DML-删除数据**

```sql
DELETE FROM 表名 [WHERE 条件];
--DELETE语句的条件可以有，也可以没有，如果没有条件，则会删除整张表的所有数据；DELETE语句不能删除某一个字段的值(可以使用UPDATE)，也就是说DELETE如果有效执行了则至少也会删除一整行的数据
```

#### (四)DQL

- **DQL-介绍**

  ​		DQL英文全称是Data Query Language (数据查询语言) ，数据查询语言，用来查询数据库中表的记录。关键字：
  ​		**SELECT**

- **DQL-语法**

```sql
--DQL编写顺序
SELECT    --字段列表
FROM      --表名列表
WHERE     --条件列表
GROUP BY  --分组字段列表
HAVING    --分组后条件列表
ORDER BY  --排序字段列表
LIMIT     --分页参数
```

- **DQL-基本查询**

```sql
SELECT 字段1，字段2，字段3…… FROM 表名;
SELECT * FROM 表名;  --*代表查询返回所有字段，但在开发中尽量不写*
SELECT 字段1[AS 别名1],字段2[AS 别名2]…… FROM 表名;  --设置别名，用于提高查询时代码的可读性，这里的AS可以省略
SELECT DISTINCT 字段列表 FROM 表名;  --去除重复记录
```

- **DQL-条件查询**

```sql
SELECT 字段列表 FROM 表名 WHERE 条件列表;  --意味着可以添加多个条件
```

​		具体条件：多个条件可以通过逻辑运算符进行组装

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-02-04 045539.png)

- **DQL-聚合函数**

  1.介绍

  ​		将一列数据作为一个整体，进行纵向计算，也就是作用于表中的某一列。

  2.常见聚合函数

  ![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-02-04 125928.png)

  ​		需要注意的是，统计数量的函数count在作用于null空数据时不会计算进去，或者说null值是不参与所有的聚合函数运算的。

  3.语法

  ```sql
  SELECT 聚合函数(字段列表) FROM 表名 [WHERE 条件列表];
  ```

- **DQL-分组查询**

```sql
SELECT 字段列表 FROM 表名 [WHERE 条件] GROUP BY 分组字段名 [HAVING 分组后过滤条件];  --查什么、从哪查、根据什么分组
--where与having区别
--1.执行时机不同:where是分组之前进行过滤，不满足where条件，不参与分组；而having是分组之后对结果进行过滤。
--2.判断条件不同:where不能对聚合函数进行判断，而having可以。
```

​		需要注意的是，sql语句的执行顺序是：
​		**where > 聚合函数 > having**
​		分组之后，查询的字段一般为聚合函数和分组字段，查询其他字段无任何意义，比如以男女分组后如果还查询姓名就只会显示一个男性姓名和一个女性姓名 (因为只有一种男性性别和一种女性性别) ，这是没有意义的。

- **DQL-排序查询**

```sql
SELECT 字段列表 FROM 表名 ORDER BY 字段1 排序方法1，字段2 排序方法2;  --支持多字段排序
```

​		排序方式只有两种：
​		**ASC：升序 (默认值，可省略)**
​		**DESC：降序**
​		注意：如果是多字段排序，当第一个字段值相同时。才会根据第二个字段进行排序，例如先按年龄排序，如果年龄相同再按入职时间排序。

- **DQL-分页查询**

```sql
SELECT 字段列表 FROM 表明 LIMIT 起始索引，查询记录数;
```

​		注意：
​		1.起始索引从0开始，起始索引=(查询页码-1)*每页显示记录数
​		2.分页查询是数据库的方言，不同的数据库有不同的实现，MySQL中是LIMIT
​		3.如果查询的是第一页数据，起始索引可以省略，直接简写为limit 10 (如果一页只显示10条数据的话)
​		4.limit需放在最后，如果sql语句中还有group by或order by的话需要将其放在limit之前

- **DQL-执行顺序**

```sql
--DQL执行顺序
FROM      --表名列表
WHERE     --条件列表
GROUP BY  --分组字段列表
HAVING    --分组后条件列表
SELECT    --字段列表
ORDER BY  --排序字段列表
LIMIT     --分页参数
```

#### (五)DCL

- **DCL-介绍**

  ​		DCL英文全称是Data Control Language (数据控制语言) ，用来管理数据库用户、控制数据库的访问权限。

- **DCL-管理用户**

```mysql
#1.查询用户
USE mysql;
SELECT * FROM user;
```

​		因为在MySQL当中，用户的信息、用户所具有的权限的信息都存放在系统数据库MySQL的user表当中，所有可以直接访问MySQL数据库并查询user表即可

​		该user表中的用户需要通过Host(主机)+User(用户名)精准访问，且Host决定了只有在哪台主机才能通过用户进行操作数据库，不能远程访问

```mysql
#2.创建用户
CREATE USER '用户名'@'主机名' IDENTIFIED BY '密码';
```

​		这里的主机名是指该用户能够用来登录其MySQL账户的主机

```mysql
#3.修改用户密码
ALTER USER '用户名'@'主机名' IDENTIFIED WITH mysql_native_password BY '新密码';
```

```mysql
#4.删除用户
DROP USER '用户名'@'主机名';
```

​		注意：
​		主机名可以使用%通配。
​		这类SQL开发人员操作的比较少，主要是DBA (Database Administrator 数据库管理员) 使用。

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-02-04 211617.png)

```mysql
SHOW GRANTS FOR '用户名'@'主机名';  #查询权限
GRANT 权限列表 ON 数据库名.表名 TO '用户名'@'主机名';  #授予权限
REVOKE 权限列表 ON 数据库名.表名 FROM '用户名'@'主机名';  #撤销权限
#tip:如果想授权或撤销权限的目标是全部数据库名和表名，则"数据库名.表名"可写为"*.*"
```

### 3.函数

​		函数是指一段可以直接被另一段程序调用的程序或代码

#### (一)字符串函数

![](C:\Users\22345\Pictures\Screenshots\屏幕截图 2026-02-06 221051.png)



#### (二)数值函数

#### (三)日期函数

#### (四)流程函数

### 4.约束



### 5.多表查询

​		

### 6.事务
